global.Honorables = global.Honorables || {};
var ROOT = global.HonorablesRoot || global.Honorables;

ROOT.Notifications = ROOT.Notifications || {};

ROOT.Notifications.onTraitUpdate = function(player, data) {

    return 0;

}